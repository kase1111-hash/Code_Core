# Ollama Automation Harness - Runtime Dependencies
# Install: pip install -r requirements.txt
# Dependencies pinned for supply chain safety (SEC-11).
# Update with: pip-compile --generate-hashes requirements.in

# Claude API client
anthropic==0.42.0

# YAML configuration parsing
PyYAML==6.0.2

# Environment variable management
python-dotenv==1.0.1

# Optional: pip install ollama-automation-harness[sentry]
# sentry-sdk==1.40.6
